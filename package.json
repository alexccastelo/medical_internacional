{
  "name": "medical-docs-viewer",
  "version": "1.0.0",
  "description": "PDF Viewer for Medical Documents",
  "scripts": {
    "build": "node generate-index.js && node build.js",
    "start": "npx serve dist"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1"
  }
}
